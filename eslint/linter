#!/bin/bash
ESLINT_FORMAT="${ESLINT_FORMAT:-stylish}"
ESLINT_ENV="${ESLINT_ENV:-node}"
npm run --silent --prefix /usr/src/linter eslint -- --env $ESLINT_ENV --format $ESLINT_FORMAT -c .eslintrc.js /usr/src/app
